name: deps

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule: [cron: "00 00 * * *"]

jobs:
  cargo-deny:
    uses: tempoxyz/ci/.github/workflows/deny.yml@a75700dae1a185e3968a902dd095c917782ced76
    permissions:
      contents: read
      id-token: write
