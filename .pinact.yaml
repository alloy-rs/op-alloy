version: 3
rules:
  # Keep the reviewed immutable deny pin; tempoxyz/ci has no release tags.
  - ignore: true
    conditions:
      - expr: ActionName == "tempoxyz/ci/.github/workflows/deny.yml" && ActionVersion == "631b2c767919910a23685267999ae812f9902e87"
